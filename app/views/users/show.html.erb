<% provide(:title, @user.name)%>

<div class="container user" style="margin-top: 40px;">
	<div class="row">
		<div class="col-md-offset-2 col-md-2 col-sm-12">
			<%= image_tag("300x200.png", :alt => "rss feed", class: "img-circle") %>
		</div>
		<div class="col-md-6">
			<h1><%= @user.name %></h1>
			<%= @user.email %>
		</div>
	</div>	
</div>
